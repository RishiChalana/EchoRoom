[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (require Docker services)
    eval: Evaluation tests (require benchmark dataset)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
